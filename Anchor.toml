[features]
seeds = false
skip-lint = false

[programs.devnet]
arbitrage_dapp = "Your_Program_ID_Here" # Update after deploy

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
